const styles = theme => ({
  root: {
    flexGrow: 1,
    maxWidth: 500,
    padding: theme.spacing.unit * 2,
    margin: theme.spacing.unit * 3
  },
  badge: {
    width: 40,
    height: 35
  }
});

export default styles;
